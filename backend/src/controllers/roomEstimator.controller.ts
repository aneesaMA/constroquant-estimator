/**
 * Room Estimator controller (Brick Wall Material Estimator).
 *
 * The active API is `POST /api/brick-wall/estimate`.
 * This file exists as a discoverable entrypoint for "room estimator" logic.
 */
export { estimateBrickWall } from "./estimator.controller";

